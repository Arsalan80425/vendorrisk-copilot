# MLOps Screenshot Placeholders

Add captured screenshots here for [docs/mlops.md](../mlops.md):

| File | Content to capture |
| --- | --- |
| `mlflow_runs.png` | MLflow UI experiment list comparing LogisticRegression and RandomForestClassifier runs |
| `mlflow_run_details.png` | Single MLflow run showing parameters, metrics, and logged artifacts |
| `drift_monitoring.png` | Terminal output from `python -m src.ml.monitor_drift` with the drift table |

After adding images, references in `docs/mlops.md` will render automatically.
